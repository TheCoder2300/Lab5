#include "defs.h"
using namespace std;

int main(){
    // initialize values
    vector<string> names;
    vector<string> majors;
    vector<string> years;
    vector<int> ids;
    int choice;
    int gamerunning;

    while gamerunning loop {
    // dropdown menu
        cout << pick choice 
        cin >> enter choice 

    //checks for input
        while loop to check for correct choice{
        }

    // choice options
        if choice 1 {
            cout << name first and last;
            getline for names;
            cout << major;
            getline for major;
            cout << year;
            cin >> year;
            cin.ignore
            cout << id num;
            cin >> id num;
               // additionally add verification for right input
        }

        if choice 2 {
                printRoll(names, major, years, ids);
        }

        if choice 3 {
           running = 1 //quits program
        }

    return 0;
}


